<h1>Display All Recipes...</h1>
<% @recipes.each do |recipe| %>
  <%= recipe.id %>
  <%= recipe.name %>
  <%= recipe.ingredients %>
  <%= recipe.cook_time %>
<% end %>

<!-- contains links to each recipe's show page... -->
